# --- Communication et API ---
requests==2.31.0           # Pour envoyer les données vers GPT-5.4
python-dotenv==1.0.1       # Pour gérer vos clés d'API en sécurité

# --- Analyse et Structure des données ---
pydantic==2.6.4            # Pour forcer le format JSON et valider les données
json-repair==0.25.0        # Utile si le LLM renvoie un JSON légèrement mal formé

# --- Audio et Streaming (RPi 5) ---
pyaudio==0.2.14            # Pour la capture micro en temps réel
numpy==1.26.4              # Requis pour le traitement de signal audio

# --- Automatisation Web (Le "Robot" de facturation) ---
playwright==1.42.0         # Pour naviguer sur les logiciels non-API
playwright-python==1.42.0

# --- Utilitaires ---
loguru==0.7.2              # Pour une gestion propre des logs (indispensable pour le débug)